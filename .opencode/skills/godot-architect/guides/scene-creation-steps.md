# 场景创建步骤指导

## ⚠️ 重要提醒：命名宪法

**在创建场景之前，请记住：**
- 场景文件名将决定其脚本类名
- player.tscn → 脚本类名必须是 Player
- enemy.tscn → 脚本类名必须是 Enemy
- ui_menu.tscn → 脚本类名必须是 UiMenu

## 🎯 基本场景创建流程

### 1. 创建根节点
1. 在编辑器中点击"场景"标签页的"+"按钮
2. 选择合适的根节点类型：
   - **Node**: 逻辑容器
   - **Node2D**: 2D场景
   - **Node3D**: 3D场景
   - **Control**: UI场景

### 2. 保存场景
1. 按 `Ctrl+S` 保存场景
2. 选择合适的目录：
   - `scenes/ui/` - UI场景
   - `scenes/characters/` - 角色场景
   - `scenes/levels/` - 关卡场景
   - `scenes/objects/` - 物体场景

### 3. 添加子节点
1. 在场景树中选择父节点
2. 点击"+"添加子节点
3. 或使用 `Ctrl+A` 快捷键

## 📝 场景组织规范

### 目录结构
```
scenes/
├── ui/              # UI界面
│   ├── main_menu.tscn
│   ├── hud.tscn
│   └── pause_menu.tscn
├── characters/      # 角色场景
│   ├── player.tscn
│   ├── enemy.tscn
│   └── npc.tscn
├── levels/          # 关卡场景
│   ├── level_01.tscn
│   ├── level_02.tscn
│   └── boss_level.tscn
├── objects/         # 物体场景
│   ├── coin.tscn
│   ├── key.tscn
│   └── chest.tscn
└── effects/         # 特效场景
    ├── explosion.tscn
    └── sparkles.tscn
```

### 节点命名规范
- **PascalCase**: 节点名称使用驼峰命名
- **前缀**: 添加功能前缀便于识别
  - `UI_` 前缀：UI节点
  - `Collision_` 前缀：碰撞形状
  - `Animation_` 前缀：动画相关

## 🔄 场景实例化

### 实例化步骤
1. 打开要添加场景的场景
2. 在文件系统中拖拽场景文件到场景树
3. 或使用"实例化场景"按钮

### 常见实例化位置
```
Main.tscn
├── World (Node2D)
│   ├── Player (Instance of player.tscn)
│   ├── Enemies (Node2D)
│   │   ├── Enemy1 (Instance of enemy.tscn)
│   │   └── Enemy2 (Instance of enemy.tscn)
│   └── Objects (Node2D)
│       ├── Coin1 (Instance of coin.tscn)
│       └── Key1 (Instance of key.tscn)
└── UI (CanvasLayer)
    ├── HUD (Instance of hud.tscn)
    └── PauseMenu (Instance of pause_menu.tscn)
```

## 💡 实用技巧

### 快速操作
- `Ctrl+S`: 保存场景
- `Ctrl+D`: 复制节点
- `Shift+Ctrl+D`: 复制并保留场景连接
- `F2`: 重命名节点
- `Ctrl+Z`: 撤销
- `Ctrl+Y`: 重做

### 场景设置
1. 右键场景根节点
2. 选择"场景设置"
3. 可以设置主场景、编辑器可见性等

### 2D场景常用设置
- **排序**: TileMap需要设置Y Sort
- **视口**: Camera2D设置跟随目标
- **碰撞层**: 设置碰撞层和掩码

### 3D场景常用设置
- **环境**: 添加WorldEnvironment节点
- **光照**: DirectionalLight用于全局照明
- **导航**: NavigationRegion3D用于AI寻路