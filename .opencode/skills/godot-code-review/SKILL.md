---
name: godot-code-review
description: >
  Godot GDScript 逐文件代码检视技能。当用户要求检视代码、review 代码、一起检视代码时使用。
  适用于 Story 完成后、提交前、或用户主动要求的代码检视场景。
  触发词：检视代码、代码检视、review、一起检视、逐文件检视。
---

# Code Review — 逐文件代码检视

与用户一起逐文件检视代码，确保每份文件经用户确认后再继续。

## 检视流程

```
收集变更文件列表 → 逐文件检视 → 在vscode中打开文件 → 用户逐文件确认 → 检视总结
```

### 1. 准备阶段

- 收集本次变更的所有文件列表（实现文件 + 测试文件 + 配置文件）
- 按逻辑顺序排列：核心基类 → 功能模块 → Autoload → 配置 → 测试
- 告知用户文件总数

### 2. 逐文件检视

对每个文件**必须**执行以下步骤，不可跳过：

1. **【强制】在 VSCode 中打开文件**：使用 Bash 执行 `code "{文件绝对路径}"`。**必须**实际执行此命令，禁止省略。macOS / Windows 均使用 `code` 命令
2. **展示变更摘要**（文件路径、变更内容、设计意图）。对于所有代码文件（`.gd`、`.tscn`、`.gdshader`、`.tres`、`.cfg` 等非普通文档），**必须**使用表格展示文件结构概览：

```
**第 N/M 个文件：{相对路径}** — {一句话描述}

| 成员类型 | 名称 | 说明 |
|---------|------|------|
| signal  | xxx  | 触发时机 |
| enum    | Xxx  | 用途说明 |
| const   | XXX  | 值及用途 |
| var     | xxx  | 类型及用途 |
| func    | xxx  | 功能说明 |

请确认或提出修改意见。
```

3. **暂停执行**，等待用户确认或提出修改意见
4. 用户确认后继续下一个文件

### 3. 检视总结

全部文件检视完毕后：

- 汇总本次检视中用户提出的所有修改意见及处理结果
- 列出经验教训（命名歧义、遗漏注释、参数遮蔽等）
- 使用 `[Skill] summarize` 对检视过程进行摘要提炼
- 将经验教训保存到 `docs/06_postmortem/MEMORY.md`
- 提取可通用的规则，更新 `CONSTITUTION.md`

## 检视检查项

对每个文件，重点关注以下方面：

### 代码规范

- [ ] **P0-3** 代码除注释外无中文
- [ ] **P0-4b** 方法（`func`）有 `##` 注释说明用途
- [ ] **P0-4b** 枚举（`enum`）及其枚举值有 `##` 注释说明含义
- [ ] **P0-4b** 信号（`signal`）有 `##` 注释说明用途
- [ ] **P0-4c** 参数名不与节点内置属性冲突（`name`、`position`、`scale` 等）
- [ ] **P0-4e** 枚举和变量命名精确反映实际用途，不含未使用的概念
- [ ] 不可达代码已删除（如 match 全覆盖后多余的 return）
- [ ] 浮点值默认设置是否合理（如音频默认音量）

### 设计规范

- [ ] **P0-4d** 复用实例模式中，生命周期入口方法（如 `enter()`）调用 `_reset()` 重置状态
- [ ] **P1-12** Autoload/Singleton 无 `class_name`；非 Singleton 脚本定义了 `class_name`
- [ ] **P1-12** 通过 `class_name` 进行类型引用，未使用 `preload`/`load` 后的变量名作类型别名
- [ ] 信号驱动通信，非直接调用
- [ ] 依赖注入而非硬编码

### 自动化验证（.gd 文件专属）

- [ ] `[MCP] minimal-godot_get_diagnostics` 诊断通过（无语法错误、类型错误）
- [ ] `[MCP] godot-ultimate_godot_lint_file` lint 通过
- [ ] `[MCP] godot-ultimate_godot_check_patterns` 模式检查通过

### 测试规范

- [ ] 使用 `add_child_autoqfree` 代替 `add_child` + `queue_free`
- [ ] 浮点比较使用 `assert_almost_eq` 而非 `assert_eq`
- [ ] 测试覆盖所有 BDD 场景
- [ ] **P1-6c** 集成测试存在于 `test/integration/{模块}/` 目录
