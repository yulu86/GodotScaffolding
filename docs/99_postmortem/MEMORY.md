# MEMORY — 经验教训

> **AI 友好的结构化经验库模板**。本项目的「避坑指南」与「最佳实践沉淀」。
>
> **AI 检索路径**（按序）：
> 1. 扫「🤖 快速检索表」按 **标签 / 关键词** 命中规则（一句话索引）
> 2. 命中后查「📐 通用原则」对应分组的 **详细规则**
> 3. 需要上下文/案例佐证时，按规则尾部 `→ 来源` 跳「📋 实例档案」
>
> **结构约定**：
> - `通用原则` = 已沉淀的跨任务规则（详细版，按主题 `#标签` 分组）
> - `实例档案` = 场景化案例（场景 / 教训 / 沉淀规则指针），按日期倒序
> - 新增经验：先看「快速检索表」是否已有同类规则 → 有则补充实例 → 无则在「通用原则」新增并加入检索表

---

## 🤖 快速检索表

> 一句话规则索引，按 `#标签` 主题聚类。命中后查「📐 通用原则」对应分组看详情。
>
> **建议标签**（按需扩展）：`#架构` `#FSM` `#数据驱动` `#碰撞` `#GdUnit4` `#测试` `#代码规范` `#性能` `#RefCounted` `#信号` `#UI` `#{自定义模块}`

| 标签 | 一句话规则 | 类型 | 来源 |
|------|----------|:---:|------|
| `#标签` | {一句话规则描述} | 通用/项目 | YYYY-MM-DD {任务名} |

<!-- 检索表新增行格式：
| `#标签` | 简明规则描述（≤30 字） | 通用 / 项目 | 日期 + 任务名 |
-->

---

## 📐 通用原则

> 已沉淀的跨任务通用规则，按 `#标签` 主题分组。详细版本，新增规则同步加入上方「快速检索表」。

### `#标签1`

- **{规则名}**：{详细描述，含 Why + How + 反例}

<!-- 原则新增分组格式：
### `#新标签`

- **规则名**：详细描述（含 Why / How / 反例）
-->

---

## 📋 实例档案

> 场景化案例（规则的来源佐证），按日期倒序。每条含：标签 / 场景 / 教训 / 沉淀规则指针。

### YYYY-MM-DD ｜ {任务/场景名}

- **标签**：`#标签1` `#标签2`
- **场景**：{在什么情况下遇到这个问题 / 做了什么决策}
- **教训**：{学到了什么，为什么会犯错，根因是什么}
- **沉淀规则**：→ 见 `#标签 规则名` / `#标签 规则名`（指向「通用原则」中的具体规则）
- **项目专属**：{仅本项目适用的特殊规则，无则省略本行}

<!-- 实例新增条目格式：
### YYYY-MM-DD ｜ {任务名}

- **标签**：`#标签1` `#标签2`
- **场景**：什么情况下遇到 / 做了什么决策
- **教训**：学到了什么 / 根因
- **沉淀规则**：→ 见 `#标签 规则名`（指向通用原则）
- **项目专属**：仅本项目适用的特殊规则（无则省略）
-->

---

## 📝 维护指南

### 新增经验的标准流程

1. **先查重**：扫「快速检索表」是否已有同类规则
2. **已有规则** → 仅在「实例档案」补充新案例（标签 / 场景 / 教训 / 指针）
3. **新规则** → ① 在「通用原则」对应 `#标签` 分组新增详细规则；② 在「快速检索表」加一行索引；③ 在「实例档案」记录来源案例

### 标签命名约定

- **小写下划线**：`#标签名`（如 `#数据驱动`、`#代码规范`）
- **主题分类**：技术领域（`#FSM`/`#碰撞`/`#UI`）、工具（`#GdUnit4`）、流程（`#架构`/`#测试`/`#代码规范`）
- **类型标记**：每条规则标「通用 / 项目」——通用跨 Godot 项目可复用，项目仅本仓库适用

### 结构原则

- **不重复**：同一规则只在「通用原则」出现一次（详细版），「实例档案」只放案例不放规则全文
- **指针互联**：实例 → 原则（`→ 见 #标签 规则名`），原则不反向指实例（避免循环）
- **倒序追加**：实例档案按日期倒序（最新在上），原则分组内顺序无关紧要
- **AI 友好**：检索表用一句话索引，详细规则在原则段；AI 扫检索表即可判断是否命中，无需读全文

---

## 示例（参考后删除）

> 以下为填充示例，实际使用时替换为本项目真实经验。

### 示例：快速检索表行

| `#FSM` | 状态机统一采用工厂式四件套（State/StateData/StateFactory/StateMachine） | 项目 | 2026-07-04 架构设计 |

### 示例：通用原则

```markdown
### `#FSM`（工厂式状态机）

- **本项目统一范式**：工厂式四件套（State/StateData/StateFactory/StateMachine），信号驱动转换，
  每次切换 `queue_free` 旧 + Factory `get_fresh_state` 新 + `call_deferred("add_child")`；
  性能瓶颈走 EP-08（对象池式 State 复用，接口不变）而非推翻范式
- **State 生命周期触发**：`enter()`/`exit()` 用 `_enter_tree`/`_exit_tree` 触发（非手动调）；
  StateMachine 必须 `call_deferred("add_child")` 让 setup 先于 enter 跑
```

### 示例：实例档案

```markdown
### 2026-07-05 ｜ M0-02 代码检视经验

- **标签**：`#FSM` `#数据驱动` `#GdUnit4` `#代码规范`
- **场景**：M0-02 完成后逐文件 code review，发现 6 处问题（is_rolling 冗余字段、player_data 弱类型...）
- **教训**：①状态标志位冗余存储会造成双源不一致；②@export 弱类型规避编译期检查、丢失 IDE 提示
- **沉淀规则**：→ 见 `#FSM 状态标志位禁止冗余` / `#数据驱动 @export 强类型`
- **项目专属**：Player 重力应用 M0-02 阶段在 player.gd 内置占位，M0-03 抽到 Movement 组件
```
